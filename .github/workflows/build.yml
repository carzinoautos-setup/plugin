name: Build React App

on:
  push:
    branches: [ main ]
    paths:
      - "plugin/carzino-plugin/frontend/**"
      - "plugin/carzino-plugin/package.json"
      - "plugin/carzino-plugin/vite.config.js"
      - ".github/workflows/build.yml"
      - "README.md"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: |
          cd plugin/carzino-plugin/frontend
          npm install

      - name: Build React app
        run: |
          cd plugin/carzino-plugin/frontend
          npm run build

      - name: Copy build output to plugin
        run: |
          rm -rf plugin/carzino-plugin/build/*
          cp -r plugin/carzino-plugin/frontend/dist/* plugin/carzino-plugin/build/

      - name: Commit build artifacts
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add plugin/carzino-plugin/build
          git commit -m "Update build artifacts" || echo "No changes to commit"
          git push
